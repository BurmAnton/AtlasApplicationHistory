# Пример настроек окружения для проекта atlas_history
#
# Скопируйте этот файл в .env и отредактируйте значения под своё окружение:
#   cp .env.example .env

# Секретный ключ Django (ОБЯЗАТЕЛЬНО заменить на уникальный боевой ключ)
DJANGO_SECRET_KEY=replace-me-with-strong-secret-key

# Режим отладки
# True  - режим разработки (подробные ошибки, не использовать в продакшене)
# False - продакшен
DJANGO_DEBUG=False

# Разрешённые хосты (через запятую, без пробелов)
# Примеры:
# DJANGO_ALLOWED_HOSTS=history.tuna-edu.ru
# DJANGO_ALLOWED_HOSTS=history.tuna-edu.ru,localhost,127.0.0.1
DJANGO_ALLOWED_HOSTS=history.tuna-edu.ru

# Доверенные источники для CSRF (через запятую)
# Для HTTPS в проде:
#   https://history.tuna-edu.ru
# Для локальной разработки можно добавить http-ссылки:
#   http://localhost:8000,http://127.0.0.1:8000
DJANGO_CSRF_TRUSTED_ORIGINS=https://history.tuna-edu.ru